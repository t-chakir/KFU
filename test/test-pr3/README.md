# Список вопросов Практической работы 3. Защита Чабанова

Посмотреть содержимое журналов фиксирующих перемещения веток и HEAD можно при помощи команды:

- [ ] a. `git log`
- [x] b. `git reflog`
- [ ] c. `git mvlog`
- [ ] d. `git branchlog`
- [ ] e. `git movelog`

---

Как восстановить файл из кэша в рабочий каталог?

- [ ] a. Используя команду `git revert <file_name>`
- [ ] b. Скопировав файл из папки `.git/cache`
- [ ] c. Используя команду `git checkout --theirs <file_name>`
- [x] d. Используя команду `git restore <file_name>`
- [ ] e. Используя команду `git reset --hard <file_name>`

---

Команда `git reset --soft <хэш_коммита>` выполнит:

- [x] a. Оставит содержимое кэша без изменения  
- [ ] b. Заменит содержимое рабочего каталога на содержимое коммита  
- [x] c. Оставит содержимое рабочего каталога без изменения  
- [x] d. Передвинет ссылку на последний коммит в текущей ветки на коммит с <хэш_коммита>  
- [ ] e. Заменит содержимое кэша на содержимое коммита  

---

Чтобы удалить файл только из кэша Git, нужно воспользоваться командой:

- [x] a. `git rm --cached <file_name>`  
- [ ] b. `git rm <file_name>`  
- [ ] c. `git rm -f <file_name>`  
- [ ] d. `git cache --rm <file_name>`  
- [ ] e. `git reset --staged <file_name>`

---

Какую команду нужно использовать, чтобы удалить ветку локального репозитория (после слияния)?

- [x] a. `git branch -d <branch_name>`  
- [ ] b. `git branch -r <branch_name>`  
- [ ] c. `git remove-branch <branch_name>`  
- [ ] d. `git branch delete <branch_name>`  
- [ ] e. `git delete -b <branch_name>`

---

Можно ли отследить хронологию изменения файла в Git репозитории?

- [ ] a. Да, можно отследить дату, автора, изменения и др. если файл указан в ".githistory"  
- [ ] b. Нет, Git не сохраняет историю  
- [ ] c. Да, можно отследить дату, автора, изменения и др., но до ближайшего слияния веток  
- [x] d. Да, можно отследить дату, автора, изменения и др. для каждого файла

---

Как называется файл (полностью), определяющий, какие неотслеживаемые файлы должны быть проигнорированы утилитой git.

> Ответ: `.gitignore`

---

Что делает команда `git add batman`?

- [ ] a. Откатывает все изменения внесённые в файл batman до состояния в котором он был добавлен в последний коммит  
- [ ] b. Создаёт в рабочем каталоге репозитория файл batman  
- [x] c. Копирует файл batman из рабочего каталога в staging секцию  
- [ ] d. Добавляет в репозиторий ветку batman  
- [ ] e. Добавляет все изменения сделанные в текущей ветке в ветку batman

---

Какая команда используется для того, чтобы создать в текущем каталоге новый репозиторий?

- [ ] a. `git repo`  
- [x] b. `git init`  
- [ ] c. `git start`  
- [ ] d. `git create`  
- [ ] e. `get new`

---

Какую команду нужно использовать, чтобы создать новую ветку?

- [x] a. `git branch <branch_name>`  
- [ ] b. `git branch -b <branch_name>`  
- [ ] c. `git branch create <branch_name>`  
- [ ] d. `git new-branch <branch_name>`  
- [ ] e. `git make-branch <branch_name>`

---

Если создать новую ветку, сделать в ней несколько коммитов и затем удалить, то, все коммиты:

- [ ] a. Удалить ветку, которую предварительно не объединили с другой, невозможно  
- [ ] b. Останутся в репозитории навсегда и будут видны в истории  
- [x] c. Будут удалены, но через некоторое время  
- [ ] d. Останутся в репозитории навсегда, но не будут видны в истории  
- [ ] e. Будут сразу же удалены

---

Что делает команда `git show` для коммита?

- [ ] a. Показывает список всех веток содержащих указанный коммит  
- [ ] b. Удаляет последний коммит из истории репозитория  
- [ ] c. Создает новую ветку на основе текущего коммита  
- [ ] d. Изменяет сообщение последнего коммита  
- [x] e. Показывает информацию о коммите

---

Как в Git установить глобально Email пользователя?

- [ ] a. `git config-global user.email <email>`  
- [ ] b. `git config user.email <email>`  
- [ ] c. `git user.email <email>`  
- [ ] d. `git --global user.email <email>`  
- [x] e. `git config --global user.email <email>`

---

Ключ, который нужно добавить к команде: 
`git commit`
Чтобы вместо создания нового коммита был перезаписан последний.
_Ответ должен быть записан маленькими буквами вместе с нужным количеством минусов._

> Ответ: `--amend`

---

Сколько людей могут работать через Git над одним проектом?

- [ ] a. Не более 50  
- [x] b. Неограниченное количество  
- [ ] c. Не более 5  
- [ ] d. Не более 30  
- [ ] e. Не более 10

---

Что делает команда `git show`?

- [ ] a. Показывает время создания репозитория  
- [ ] b. Показывает состояние проекта  
- [ ] c. Показывает содержимое файла  
- [x] d. Показывает изменения, сделанные в коммите

---

Что делает команда `git status`?

- [ ] a. Показывает имя и email нашего пользователя, а также является ли он авторизованным в системе GitHub или нет  
- [ ] b. Такой команды нет  
- [x] c. Показывает состояние проекта: кол-во untracked, deleted, new и прочих файлов, количество коммитов, на которое отличается локальная версия репозитория от удаленного и так далее  
- [ ] d. Показывает место, занимаемое репозиторием на жестком диске и кол-во выделенного под репозиторий месте

---

Вы попробовали выполнить слияние, но получили конфликт. Как откатить слияние?

- [ ] a. `git revert`  
- [ ] b. `git reset --merge`  
- [ ] c. `git merge --continue`  
- [ ] d. Конфликт слияния не возможно откатить  
- [x] e. `git merge --abort`

---

Как узнать, кто автор строчки в файле, используя утилиту git?

- [x] a. Команда `git blame <file_name>`  
- [ ] b. Команда `git status <file_name>`  
- [ ] c. Команда `git commit --author <file_name>`  
- [ ] d. Команда `git show --author <file_name>`

---

При помощи какой команды можно получить копию удалённого репозитория?

- [x] a. `git clone <url>`  
- [ ] b. `git copy <url>`  
- [ ] c. `git get <url>`  
- [ ] d. `git download <url>`

---

Во время создания новой ветки командой: `git branch <branch_name>`, она

- [ ] a. ответвляется от ветки master (или main)  
- [ ] b. не ответвляется ни от чего, а создаётся как полностью отдельная от остальных  
- [ ] c. ответвляется от корневого коммита в репозитории  
- [ ] d. объединяет две или более других веток в одну  
- [x] e. ответвляется от текущей ветки

---

Вы находитесь в ветке С и хотите влить изменения из ветки А в ветку В. Что для этого нужно сделать?

- [ ] a. Просто ввести команду: `git pull A B`  
- [x] b. Перейти в ветку B и выполнить команду: `git merge A`  
- [ ] c. Просто ввести команду: `git rebase A B`  
- [ ] d. Перейти в ветку A и выполнить команду: `git merge B`  
- [ ] e. Просто ввести команду: `git merge A B`

---

Как сделать коммит?

- [ ] a. Набрать команду `git commit` и перечислить файлы, которые должны быть добавлены в коммит. Например так: `git commit a.file b.file`  
- [ ] b. Набрать команду `git commit` в любой момент времени  
- [x] c. Набрать команду `git commit`, но только после того, как файлы, которые должны быть добавлены в коммит были добавлены в кэш. Например так: `git add a.file b.file`  
- [ ] d. Нельзя сделать коммит, ведь такого понятия не существует

---

Чтобы посмотреть список файлов, находящихся в кэше, нужно воспользоваться командой:

- [ ] a. `git cache`  
- [ ] b. `ls -a`  
- [ ] c. `git ls-tree`  
- [x] d. `git ls-files`  
- [ ] e. `git show --cache`

---

Команда `git reset --hard <хэш_коммита>` выполнит:

- [ ] a. Оставит содержимое кэша без изменения  
- [ ] b. Оставит содержимое рабочего каталога без изменения  
- [x] c. Передвинет ссылку на последний коммит в текущей ветки на коммит с <хэш_коммита>  
- [x] d. Заменит содержимое кэша на содержимое коммита  
- [x] e. Заменит содержимое рабочего каталога на содержимое коммита

---

Какую команду нужно использовать, чтобы переключиться на ветку `<branch_name>`?

- [ ] a. `git commit <branch_name>`  
- [ ] b. `git merge <branch_name>`  
- [x] c. `git switch <branch_name>`  
- [ ] d. `git branch <branch_name>`  
- [ ] e. `git pull origin <branch_name>`

---

Команда `git reset <хэш_коммита>` выполнит:

- [ ] a. Заменит содержимое рабочего каталога на содержимое коммита  
- [x] b. Оставит содержимое рабочего каталога без изменения  
- [x] c. Оставит содержимое кэша без изменения  
- [ ] d. Передвинет ссылку на последний коммит в текущей ветки на коммит с <хэш_коммита>  
- [x] e. Заменит содержимое кэша на содержимое коммита

---

Где Git хранит файлы, относящиеся к репозиторию, созданному в текущей папке?

- [ ] a. Утилита Git вообще никак не относится к репозиториям. Она нужна для другого.  
- [x] b. В текущей папке в каталоге ".git"  
- [ ] c. В системной папке ".git"  
- [ ] d. Файлы не хранятся локально. Они расположены на github.com

---

Если удалить ветку (предварительно влитую в другую), все коммиты в этой ветке:

- [ ] a. Будут удалены, но через некоторое время  
- [ ] b. Если ветку объединили с другой, то её уже невозможно удалить  
- [x] c. Останутся в репозитории навсегда и будут видны в истории  
- [ ] d. Останутся в репозитории навсегда, но не будут видны в истории  
- [ ] e. Будут сразу же удалены

---

Как решить конфликт слияния в Git?

- [ ] a. Выполнить команду `git merge please`  
- [x] b. Руками поправить изменения там, где Git не смог это сделать автоматически и затем собрать всё в коммит  
- [ ] c. Выполнить команду `git revert`  
- [ ] d. Никак, придется создавать репозиторий заново

---

у админа перерыв...
